-- local nvim = require("nvim.api")

describe("flashcard", function()
	-- it("can add new cards", function()
	-- 	-- TODO
	-- 	local result = require("flashcard").addCard()
	-- 	vim.api.nvim_feedkeys("q", "n", true)
	-- 	assert.are.same("TODO add things", result)
	-- end)
	--
	-- it("can study cards", function()
	-- 	-- TODO
	-- 	local result = require("flashcard").study()
	-- 	assert.are.same("TODO study things", result)
	-- end)
end)
